from setuptools import setup, find_packages

setup(
    name="offline_pos_erpnext",
    version="0.0.1",
    description="Offline POS for ERPNext",
    author="Your Name",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[]
)
